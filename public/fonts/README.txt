Place your self-hosted Geist variable fonts here so builds don’t fetch Google Fonts.

Expected files:
- Geist-Variable.woff2
- GeistMono-Variable.woff2

After adding them, Next.js will load them locally via next/font/local.
