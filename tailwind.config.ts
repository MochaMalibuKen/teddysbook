/**
 * Tailwind v4 ignores config files at build time.
 * Theme tokens now live in app/globals.css via @theme so classes like bg-teddy-blue work.
 * Keep this file only if your editor needs Tailwind intellisense.
 */
const config = {};

export default config;
